spring:
  application:
    name: api-gateway

  config:
    import: configserver:http://localhost:8001

  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true

